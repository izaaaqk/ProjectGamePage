You must have flask and python installed.
To get flask running properly you must set these two environment variables:
export FLASK_ENV=development
export FLASK_APP=app
then run the command: flask run
